#include "header.h"
#include <stdio.h>

void add(leak_t record){
   for(int i = 0; i > 50; i++){
       if(alloc_table[i].bytes == NULL){
          
       }
   }
}

void remove(leak_t record){

}

void* find(int* ptr){

}
